{
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "scripts": {
  "build": "npx tailwindcss -i ./src/input.css -o ./dist/style.css",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
}

}
